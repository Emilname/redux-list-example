import React from "react";

const DeleteCollection = ({ onClick }) => {
  return (
    <button style={{ color: "red" }} onClick={onClick}>
      D
    </button>
  );
};

export default DeleteCollection;
